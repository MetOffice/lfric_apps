name: Validate Symlinks

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  validate_symlinks:
    permissions:
      contents: read  # Required to evaluate the event trigger metadata safely
    uses: MetOffice/growss/.github/workflows/validate_symlinks.yaml@develop  # zizmor: ignore[unpinned-uses]
