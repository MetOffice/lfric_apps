@@ -1,6 +1,6 @@
 module adj_apply_mixed_lu_operator_kernel_mod
   use argument_mod, only : arg_type, cell_column, gh_field, gh_inc, gh_operator, gh_read, gh_readwrite, gh_real
-  use constants_mod, only : i_def, r_def
+  use constants_mod, only : i_def, r_solver
   use kernel_mod, only : kernel_type
   use fs_continuity_mod, only : w2, w3, wtheta
   implicit none
@@ -40,24 +40,24 @@
     integer(kind=i_def), dimension(ndf_w2), intent(in) :: map_w2
     integer(kind=i_def), dimension(ndf_wt), intent(in) :: map_wt
     integer(kind=i_def), dimension(ndf_w3), intent(in) :: map_w3
-    real(kind=r_def), dimension(undf_w2), intent(in) :: lhs_u
-    real(kind=r_def), dimension(undf_w2), intent(inout) :: wind
-    real(kind=r_def), dimension(undf_w2), intent(in) :: norm_u
-    real(kind=r_def), dimension(undf_wt), intent(inout) :: theta
-    real(kind=r_def), dimension(undf_w3), intent(inout) :: exner
-    real(kind=r_def), dimension(ndf_w2,ndf_w2,ncell1), intent(in) :: mu_cd
-    real(kind=r_def), dimension(ndf_w2,ndf_wt,ncell2), intent(in) :: p2theta
-    real(kind=r_def), dimension(ndf_w2,ndf_w3,ncell3), intent(in) :: grad
+    real(kind=r_solver), dimension(undf_w2), intent(in) :: lhs_u
+    real(kind=r_solver), dimension(undf_w2), intent(inout) :: wind
+    real(kind=r_solver), dimension(undf_w2), intent(in) :: norm_u
+    real(kind=r_solver), dimension(undf_wt), intent(inout) :: theta
+    real(kind=r_solver), dimension(undf_w3), intent(inout) :: exner
+    real(kind=r_solver), dimension(ndf_w2,ndf_w2,ncell1), intent(in) :: mu_cd
+    real(kind=r_solver), dimension(ndf_w2,ndf_wt,ncell2), intent(in) :: p2theta
+    real(kind=r_solver), dimension(ndf_w2,ndf_w3,ncell3), intent(in) :: grad
     integer(kind=i_def) :: df
     integer(kind=i_def) :: k
     integer(kind=i_def) :: ik
-    real(kind=r_def), dimension(ndf_w2) :: u_e
-    real(kind=r_def), dimension(ndf_w2) :: lhs_e
-    real(kind=r_def), dimension(ndf_wt) :: t_e
-    real(kind=r_def), dimension(ndf_w3) :: p_e
-    real(kind=r_def), dimension(ndf_w2) :: mucd_ue
-    real(kind=r_def), dimension(ndf_w2) :: p2t_te
-    real(kind=r_def), dimension(ndf_w2) :: grad_pe
+    real(kind=r_solver), dimension(ndf_w2) :: u_e
+    real(kind=r_solver), dimension(ndf_w2) :: lhs_e
+    real(kind=r_solver), dimension(ndf_wt) :: t_e
+    real(kind=r_solver), dimension(ndf_w3) :: p_e
+    real(kind=r_solver), dimension(ndf_w2) :: mucd_ue
+    real(kind=r_solver), dimension(ndf_w2) :: p2t_te
+    real(kind=r_solver), dimension(ndf_w2) :: grad_pe
     integer :: idx
     integer :: i
     integer :: j
@@ -66,13 +66,13 @@
     integer :: i_2
     integer :: j_2
 
-    lhs_e = 0.0_r_def
-    u_e = 0.0_r_def
-    p_e = 0.0_r_def
-    t_e = 0.0_r_def
-    mucd_ue = 0.0_r_def
-    p2t_te = 0.0_r_def
-    grad_pe = 0.0_r_def
+    lhs_e = 0.0_r_solver
+    u_e = 0.0_r_solver
+    p_e = 0.0_r_solver
+    t_e = 0.0_r_solver
+    mucd_ue = 0.0_r_solver
+    p2t_te = 0.0_r_solver
+    grad_pe = 0.0_r_solver
     do k = nlayers - 1, 0, -1
       ik = cell * nlayers + k - nlayers + 1
       do df = ndf_w2, 1, -1
