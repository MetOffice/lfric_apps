@@ -104,7 +104,11 @@
   integer(kind=i_def) :: k
   real(kind=r_def)    :: u, v, dtdx, dtdy
   real(kind=r_def)    :: ls_u, ls_v, ls_dtdx, ls_dtdy
+  real(kind=r_def)    :: ls_dtdx_1, ls_dtdy_1
+  real(kind=r_def)    :: ls_dtdx_2, ls_dtdy_2
+  real(kind=r_def)    :: ls_u_1, ls_v_1
+  real(kind=r_def)    :: ls_u_2, ls_v_2
 
   ! Horizontal advective update computation
 
   ! Bottom point
@@ -125,16 +129,16 @@
   ! Middle points
   do k = 1, nlayers - 1
     ! Linearisation state
-    ls_u =  0.25_r_def * ( ls_wind(map_w2(1) + k - 1 ) &
+    ls_u_2 =  0.25_r_def * ( ls_wind(map_w2(1) + k - 1 ) &
                          + ls_wind(map_w2(1) + k )     &
                          + ls_wind(map_w2(3) + k - 1 ) &
                          + ls_wind(map_w2(3) + k ) )
-    ls_v = -0.25_r_def * ( ls_wind(map_w2(2) + k - 1 ) &
+    ls_v_2 = -0.25_r_def * ( ls_wind(map_w2(2) + k - 1 ) &
                          + ls_wind(map_w2(2) + k )     &
                          + ls_wind(map_w2(4) + k - 1 ) &
                          + ls_wind(map_w2(4) + k ) )
-    ls_dtdx = ls_tracer(map_w1(3) + k) - ls_tracer(map_w1(1) + k)
-    ls_dtdy = ls_tracer(map_w1(4) + k) - ls_tracer(map_w1(2) + k)
+    ls_dtdx_2 = ls_tracer(map_w1(3) + k) - ls_tracer(map_w1(1) + k)
+    ls_dtdy_2 = ls_tracer(map_w1(4) + k) - ls_tracer(map_w1(2) + k)
     ! Perturbation
     u =  0.25_r_def * ( wind(map_w2(1) + k - 1 ) &
                       + wind(map_w2(1) + k )     &
@@ -147,25 +151,25 @@
     dtdx = tracer(map_w1(3) + k) - tracer(map_w1(1) + k)
     dtdy = tracer(map_w1(4) + k) - tracer(map_w1(2) + k)
     ! Calculation
-    advective(map_wt(1)+k) = ls_u * dtdx + ls_v * dtdy + &
-                             u * ls_dtdx + v * ls_dtdy
+    advective(map_wt(1)+k) = ls_u_2 * dtdx + ls_v_2 * dtdy + &
+                             u * ls_dtdx_2 + v * ls_dtdy_2
   end do
 
   ! Final point on the top
   k = nlayers-1
   ! Linearisation state
-  ls_u =  0.25_r_def * ( ls_wind(map_w2(1) + k) + ls_wind(map_w2(3) + k) )
-  ls_v = -0.25_r_def * ( ls_wind(map_w2(2) + k) + ls_wind(map_w2(4) + k) )
-  ls_dtdx = ls_tracer(map_w1(11) + k) - ls_tracer(map_w1(9) + k)
-  ls_dtdy = ls_tracer(map_w1(12) + k) - ls_tracer(map_w1(10) + k)
+  ls_u_1 =  0.25_r_def * ( ls_wind(map_w2(1) + k) + ls_wind(map_w2(3) + k) )
+  ls_v_1 = -0.25_r_def * ( ls_wind(map_w2(2) + k) + ls_wind(map_w2(4) + k) )
+  ls_dtdx_1 = ls_tracer(map_w1(11) + k) - ls_tracer(map_w1(9) + k)
+  ls_dtdy_1 = ls_tracer(map_w1(12) + k) - ls_tracer(map_w1(10) + k)
   ! Perturbation
   u =  0.25_r_def * ( wind(map_w2(1) + k) + wind(map_w2(3) + k) )
   v = -0.25_r_def * ( wind(map_w2(2) + k) + wind(map_w2(4) + k) )
   dtdx = tracer(map_w1(11) + k) - tracer(map_w1(9) + k)
   dtdy = tracer(map_w1(12) + k) - tracer(map_w1(10) + k)
   ! Calculation
-  advective(map_wt(2)+k) = ls_u * dtdx + ls_v * dtdy + &
-                           u * ls_dtdx + v * ls_dtdy
+  advective(map_wt(2)+k) = ls_u_1 * dtdx + ls_v_1 * dtdy + &
+                           u * ls_dtdx_1 + v * ls_dtdy_1
 
 end subroutine tl_poly_advective_code
 
